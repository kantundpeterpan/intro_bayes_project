model {
  for (i in 1:N) {
    y[i] ~ dbin(pi[i], n[i])
    logit(pi[i]) <- alpha0 + alpha1 * X[i,1] + alpha2 * X[i,3]
  }

  alpha0 ~ dnorm(0, 0.001)
  alpha1 ~ dnorm(0, 0.001)
  alpha2 ~ dnorm(0, 0.001)
}