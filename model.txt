
model {
  for (i in 1:N) {
    y[i] ~ dbin(p[i], SampleSize[i])
    logit(p[i]) <- alpha0 + alpha1 * Medicaid[i] + alpha2 * Uninsured[i]
  }

  alpha0 ~ dnorm(0, 0.001)
  alpha1 ~ dnorm(0, 0.001)
  alpha2 ~ dnorm(0, 0.001)
}

